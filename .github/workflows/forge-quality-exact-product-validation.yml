name: Forge Quality exact-product validation

on:
  pull_request:
    paths:
      - .github/workflows/forge-quality-exact-product-validation.yml

permissions:
  contents: read

jobs:
  exact-product:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 2

      - name: Assert exact product parent
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD^)" = "bd6826cb6cab4fbf551398d88656979eb8c574fb"
          git diff --check HEAD^

      - name: Debug Forge Quality contracts
        shell: bash
        run: |
          set -euo pipefail
          swift test \
            --package-path Packages/ForgeQualityCore \
            -Xswiftc -warnings-as-errors

      - name: Release Forge Quality contracts
        shell: bash
        run: |
          set -euo pipefail
          swift test \
            --package-path Packages/ForgeQualityCore \
            -c release \
            -Xswiftc -warnings-as-errors
