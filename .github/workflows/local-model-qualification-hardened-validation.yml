name: Local Model qualification hardened exact-product validation

on:
  pull_request:
    paths:
      - ".github/workflows/local-model-qualification-hardened-validation.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Local Model qualification hardened exact product
    runs-on: macos-26
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 2
      - name: Assert exact product parent and validation-only scope
        shell: bash
        run: |
          set -euo pipefail
          expected_product="dff8d998443767b5a1314795d3b3e0cfa5913828"
          actual_parent="$(git rev-parse HEAD^)"
          test "$actual_parent" = "$expected_product"
          changed="$(git diff --name-only "$expected_product"..HEAD)"
          test "$changed" = ".github/workflows/local-model-qualification-hardened-validation.yml"
          git diff --check "$expected_product"..HEAD
      - name: Test LocalModelQualificationCore Debug
        run: swift test --package-path Packages/LocalModelQualificationCore -Xswiftc -warnings-as-errors
      - name: Test LocalModelQualificationCore Release
        run: swift test --package-path Packages/LocalModelQualificationCore -c release -Xswiftc -warnings-as-errors
