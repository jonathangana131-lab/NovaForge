{"category":"codeRepair","expected":{"files":[{"content":"let score = 0;\nexport function addPoint(){ score += 1; return score; }\nexport function restart(){ score = 0; return score; }\n","path":"game.js"}]},"instructions":"Repair the counter so restart deterministically restores score to zero without changing the public function names.","revision":1,"schema_version":1,"task_id":"v14.repair.restart-counter","workspace":[{"content":"let score = 0;\nexport function addPoint(){ score += 1; return score; }\nexport function restart(){ score == 0; return score; }\n","path":"game.js"}]}
