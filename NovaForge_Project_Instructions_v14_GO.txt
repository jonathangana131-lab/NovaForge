NOVAFORGE PROJECT INSTRUCTIONS — AUTONOMOUS MODE

Repository: jonathangana131-lab/NovaForge
Execution authority: root `AGENTS.md`

The previous NF-SWARM-v14 execution system is retired for normal development. Do not restore worker quotas, swarm lanes/claims, recovery-branch ladders, capacity filling, or synthetic stop authority because historical V14 files still exist in the repository.

WHEN THE USER SAYS `GO`, `continue`, `work on NovaForge`, `finish NovaForge`, OR EQUIVALENT:
- begin actual GitHub/code work immediately;
- read current `AGENTS.md`;
- refresh live `main`, open PRs, CI/reviews, recent commits, and relevant current product/local-AI docs;
- finish a strong existing near-merge candidate before creating a competing implementation when practical;
- otherwise choose the highest-value current blocker to a coherent product/release outcome;
- implement, test/build/run/inspect as appropriate, fix findings, merge when accepted, refresh, and continue;
- do not stop after only a plan, issue, commit, PR, review, test, or merge while another useful safe outcome is available in the current execution window.

PRODUCT NORTH STAR:
NovaForge is an iPhone-native, local-first AI creation OS. The normal loop is Describe -> Understand -> Forge -> Run -> Experience -> Critique -> Repair -> Polish -> Complete. Composer/Plan Space, Full Forge autonomy, generated-runtime playtest/repair, durable projects, native visual quality, and local AI remain flagship product directions.

LOCAL AI / QWEN:
Treat active Qwen 3.8 work as part of the NovaForge product. Converge it into the main app/runtime rather than creating an endless family of validation/recovery branches. Never invent exact iPhone compatibility, memory, speed, thermal, energy, context, or quality claims; physical-device claims require exact physical-device evidence. Local Only must never silently use cloud.

COORDINATION:
Use current GitHub branches, PRs, checks, reviews, and code as coordination. There is no fixed agent count. Default to one implementation per overlapping subsystem and use parallel agents only for clearly independent work. Fewer strong, mergeable branches are better than a large worker population.

QUALITY:
Run relevant tests/build/runtime checks for each change. User-visible iOS work should be run and visually inspected when tooling permits. Reserve whole-product release sweeps, long stress runs, full accessibility/performance qualification, and physical-device model qualification for the changes/milestones that actually require them. Never weaken a gate merely to get green.

SOURCE OF TRUTH:
Live GitHub and exact evidence > stale continuation text, historical worker metadata, old branch names, or swarm paperwork.
