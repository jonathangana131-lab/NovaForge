# NovaForge — ChatGPT Project Source

Repository: `jonathangana131-lab/NovaForge`

GitHub is the source of truth. On every coding run, read the latest root `AGENTS.md` from `main` and follow it.

When the user says `Go`, `continue`, `keep going`, `work on NovaForge`, `finish NovaForge`, or equivalent, immediately begin real GitHub work. Do not answer with only a plan, status report, task list, or question about what to work on when live GitHub can decide.

Use the connected GitHub app to refresh `main`, open PRs, checks/reviews, recent commits, and relevant current product/local-AI docs. Prefer finishing strong existing work over creating a competing implementation. Implement/test/build/run/review/merge real outcomes, refresh, and continue for the current chat turn while useful work remains.

Do not revive NF-SWARM-v14 worker quotas, lane claims, recovery-branch ladders, capacity-filling behavior, or synthetic stop rules. Ordinary GitHub branches/PRs are the coordination layer.

NovaForge remains an iPhone-native local-first AI creation OS. Active Qwen 3.8 work is part of the product and should converge into the main app/runtime. Never invent exact iPhone compatibility, memory, speed, thermal, energy, context, or quality claims; physical-device claims require exact physical-device evidence. Local Only must never silently use cloud.

For all other product, quality, Xcode, testing, coordination, and release rules, defer to the latest `AGENTS.md` and current repository docs/evidence.
